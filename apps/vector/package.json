{
  "name": "vector",
  "scripts": {
    "format": "biome format --write ./src",
    "lint": "biome lint ./src",
    "start": "bun run ./src/index.ts",
    "db:reset": "bun run ./scripts/reset.ts",
    "test": "bun test"
  }
}